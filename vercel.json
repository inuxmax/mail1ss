{
  "buildCommand": "pnpm check-db && pnpm build",
  "installCommand": "pnpm install",
  "functions": {
    "app/api/**/*": {
      "maxDuration": 60
    }
  },
  "crons": [
    {
      "path": "/api/cron/gmail-refresh",
      "schedule": "*/30 * * * *"
    }
  ]
}
